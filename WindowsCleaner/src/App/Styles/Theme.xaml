<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═══════════════════ COLORS ════════════════════════════════════════ -->
    <Color x:Key="BackgroundColor">#0D0D14</Color>
    <Color x:Key="SidebarColor">#111119</Color>
    <Color x:Key="CardColor">#1A1A2E</Color>
    <Color x:Key="CardHoverColor">#21213A</Color>
    <Color x:Key="AccentPurpleColor">#7C6AF7</Color>
    <Color x:Key="AccentTealColor">#2AD0CA</Color>
    <Color x:Key="PrimaryTextColor">#F2F2FF</Color>
    <Color x:Key="SecondaryTextColor">#8F8FB0</Color>
    <Color x:Key="TertiaryTextColor">#4A4A6A</Color>
    <Color x:Key="BorderColor">#252540</Color>
    <Color x:Key="SuccessColor">#22C55E</Color>
    <Color x:Key="WarningColor">#FDA44F</Color>
    <Color x:Key="ErrorColor">#F94444</Color>
    <Color x:Key="AccentPurpleLightColor">#9B8BFF</Color>

    <!-- ═══════════════════ SOLID BRUSHES ═════════════════════════════════ -->
    <SolidColorBrush x:Key="BackgroundBrush"        Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="SidebarBrush"           Color="{StaticResource SidebarColor}"/>
    <SolidColorBrush x:Key="CardBrush"              Color="{StaticResource CardColor}"/>
    <SolidColorBrush x:Key="CardHoverBrush"         Color="{StaticResource CardHoverColor}"/>
    <SolidColorBrush x:Key="AccentPurpleBrush"      Color="{StaticResource AccentPurpleColor}"/>
    <SolidColorBrush x:Key="AccentTealBrush"        Color="{StaticResource AccentTealColor}"/>
    <SolidColorBrush x:Key="PrimaryTextBrush"       Color="{StaticResource PrimaryTextColor}"/>
    <SolidColorBrush x:Key="SecondaryTextBrush"     Color="{StaticResource SecondaryTextColor}"/>
    <SolidColorBrush x:Key="TertiaryTextBrush"      Color="{StaticResource TertiaryTextColor}"/>
    <SolidColorBrush x:Key="AppBorderBrush"         Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="SuccessBrush"           Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="WarningBrush"           Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="ErrorBrush"             Color="{StaticResource ErrorColor}"/>
    <SolidColorBrush x:Key="AccentPurpleLightBrush" Color="{StaticResource AccentPurpleLightColor}"/>

    <!-- Semi-transparent overlay brushes -->
    <SolidColorBrush x:Key="AccentPurpleSubtleBrush" Color="#207C6AF7"/>
    <SolidColorBrush x:Key="AccentTealSubtleBrush"   Color="#202AD0CA"/>
    <SolidColorBrush x:Key="SuccessSubtleBrush"      Color="#2022C55E"/>
    <SolidColorBrush x:Key="ErrorSubtleBrush"        Color="#20F94444"/>
    <SolidColorBrush x:Key="WarningSubtleBrush"      Color="#20FDA44F"/>

    <!-- ═══════════════════ TYPOGRAPHY STYLES ═════════════════════════════ -->

    <!-- 28px Bold — page hero title -->
    <Style x:Key="HeroTitleStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"   Value="Segoe UI Variable Display, Segoe UI"/>
        <Setter Property="FontSize"     Value="28"/>
        <Setter Property="FontWeight"   Value="Bold"/>
        <Setter Property="Foreground"   Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <!-- 22px SemiBold — page section title -->
    <Style x:Key="LargeTitleStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"   Value="Segoe UI Variable Display, Segoe UI"/>
        <Setter Property="FontSize"     Value="22"/>
        <Setter Property="FontWeight"   Value="SemiBold"/>
        <Setter Property="Foreground"   Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <!-- 18px SemiBold — card heading -->
    <Style x:Key="TitleStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"   Value="Segoe UI Variable, Segoe UI"/>
        <Setter Property="FontSize"     Value="18"/>
        <Setter Property="FontWeight"   Value="SemiBold"/>
        <Setter Property="Foreground"   Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <!-- 15px SemiBold — sub-heading -->
    <Style x:Key="HeadingStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"   Value="Segoe UI Variable, Segoe UI"/>
        <Setter Property="FontSize"     Value="15"/>
        <Setter Property="FontWeight"   Value="SemiBold"/>
        <Setter Property="Foreground"   Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <!-- 13px Regular — body text -->
    <Style x:Key="BodyStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"   Value="Segoe UI Variable Text, Segoe UI"/>
        <Setter Property="FontSize"     Value="13"/>
        <Setter Property="FontWeight"   Value="Normal"/>
        <Setter Property="Foreground"   Value="{StaticResource SecondaryTextBrush}"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <!-- 11px Regular — captions, tooltips -->
    <Style x:Key="CaptionStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"   Value="Segoe UI Variable Small, Segoe UI"/>
        <Setter Property="FontSize"     Value="11"/>
        <Setter Property="FontWeight"   Value="Normal"/>
        <Setter Property="Foreground"   Value="{StaticResource TertiaryTextBrush}"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <!-- 10px Bold tracked — section label in uppercase -->
    <Style x:Key="OverlineStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"       Value="Segoe UI Variable Small, Segoe UI"/>
        <Setter Property="FontSize"         Value="10"/>
        <Setter Property="FontWeight"       Value="Bold"/>
        <Setter Property="Foreground"       Value="{StaticResource TertiaryTextBrush}"/>
        <!-- WPF does not have CharacterSpacing; use CAPS text in XAML for visual separation -->
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <!-- 36px Bold — dashboard metric numbers -->
    <Style x:Key="MetricLargeStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"   Value="Segoe UI Variable Display, Segoe UI"/>
        <Setter Property="FontSize"     Value="36"/>
        <Setter Property="FontWeight"   Value="Bold"/>
        <Setter Property="Foreground"   Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <!-- 24px SemiBold — secondary metrics -->
    <Style x:Key="MetricMediumStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"   Value="Segoe UI Variable Display, Segoe UI"/>
        <Setter Property="FontSize"     Value="24"/>
        <Setter Property="FontWeight"   Value="SemiBold"/>
        <Setter Property="Foreground"   Value="{StaticResource AccentPurpleBrush}"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

</ResourceDictionary>

<UserControl x:Class="WinSweep.Views.CacheCleanerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WinSweep.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:CacheCleanerViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- page header -->
            <RowDefinition Height="*"/>      <!-- card grid -->
            <RowDefinition Height="Auto"/>   <!-- status bar -->
        </Grid.RowDefinitions>

        <!-- ── Header ────────────────────────────────────────────────────── -->
        <Border Grid.Row="0" Padding="28,22,24,18"
                BorderBrush="{StaticResource AppBorderBrush}"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Cache Cleaner"
                               Style="{StaticResource LargeTitleStyle}"/>
                    <TextBlock Text="Scan and clear browser caches, Windows Update, thumbnails, DNS, and Prefetch."
                               Style="{StaticResource BodyStyle}"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Scan All"
                            Command="{Binding ScanAllCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,8,0"/>
                    <Button Content="Clean All"
                            Command="{Binding CleanAllCommand}"
                            Style="{StaticResource DangerButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── Cache category cards ──────────────────────────────────────── -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Padding="24,20,24,0">
            <ItemsControl ItemsSource="{Binding Cards}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Individual cache card -->
                        <Border Width="260" Margin="0,0,16,16"
                                Style="{StaticResource CardStyle}" Padding="18">
                            <StackPanel>
                                <!-- Icon -->
                                <TextBlock Text="{Binding Icon}"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="26"
                                           Foreground="{StaticResource AccentPurpleBrush}"
                                           Margin="0,0,0,10"/>

                                <!-- Name + desc -->
                                <TextBlock Text="{Binding Name}"
                                           Style="{StaticResource HeadingStyle}"
                                           FontSize="14"/>
                                <TextBlock Text="{Binding Description}"
                                           Style="{StaticResource CaptionStyle}"
                                           TextWrapping="Wrap"
                                           Margin="0,4,0,12"/>

                                <!-- Size / status badge -->
                                <Border CornerRadius="4" Padding="8,4"
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,12">
                                    <Border.Background>
                                        <SolidColorBrush Color="#1A1A3A"/>
                                    </Border.Background>
                                    <TextBlock Text="{Binding StatusLabel}"
                                               FontFamily="Segoe UI Variable Small, Segoe UI"
                                               FontSize="11" FontWeight="SemiBold"
                                               Foreground="{StaticResource AccentTealBrush}"/>
                                </Border>

                                <!-- Freed label (visible after clean) -->
                                <TextBlock Text="{Binding FormattedFreed}"
                                           FontFamily="Segoe UI Variable Small, Segoe UI"
                                           FontSize="11"
                                           Foreground="{StaticResource SuccessBrush}"
                                           Margin="0,0,0,12"
                                           Visibility="{Binding HasSize,
                                               Converter={StaticResource BoolToVisConverter}}"/>

                                <!-- Buttons -->
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Scan"
                                            Command="{Binding ScanCommand}"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Padding="12,6" FontSize="12"
                                            Margin="0,0,8,0"/>
                                    <Button Content="Clean"
                                            Command="{Binding CleanCommand}"
                                            Style="{StaticResource DangerButtonStyle}"
                                            Padding="12,6" FontSize="12"
                                            IsEnabled="{Binding CanClean}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- ── Status bar ─────────────────────────────────────────────────── -->
        <Border Grid.Row="2"
                Background="{StaticResource SidebarBrush}"
                BorderBrush="{StaticResource AppBorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="24,10">
            <TextBlock Text="{Binding StatusText}"
                       Style="{StaticResource BodyStyle}"
                       VerticalAlignment="Center"/>
        </Border>

    </Grid>
</UserControl>
